# Dockerfile for City Brain System
FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 复制依赖文件
COPY requirements.txt .

# 安装依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目文件
COPY . .

# 暴露端口
EXPOSE 9003

# 启动命令
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "9003"]