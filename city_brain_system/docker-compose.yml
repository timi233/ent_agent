version: '3.8'

services:
  city-brain-system:
    build: .
    ports:
      - "8000:8000"
    environment:
      - BOCHA_API_KEY=${BOCHA_API_KEY}
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    volumes:
      - .env:/app/.env
    restart: unless-stopped